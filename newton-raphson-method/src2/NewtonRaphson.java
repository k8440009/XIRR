package src2;

import src2.data.NewtonData;

public class NewtonRaphson {
    public void initialize(NewtonData data){

    }
    // 초기화
    
    public boolean executeMethod(NewtonData data, double rate){
        /*
        for (int iterations = 0; iterations < 20; iterations++){
            double stepSize;

            if (rate < data.getRateMin() || rate > data.getRateMax()){
                return false;
            }

            
        }
        */

    }
}
